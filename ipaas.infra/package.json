{
  "name": "ipaas-infra",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Postgres infrastructure and schema migrations for the iPaaS platform — the database itself, plus the schema every engine (batch Orchestration Engine today, the future real-time engine later) connects to.",
  "scripts": {
    "migrate:up": "node-pg-migrate up",
    "migrate:down": "node-pg-migrate down",
    "migrate:create": "node-pg-migrate create"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "node-pg-migrate": "^9.0.0",
    "pg": "^8.22.0"
  }
}
